Implement `slugify(input)` in `slugify.mjs` so it turns any string into a URL slug:

- lowercase everything
- words separated by single hyphens
- no punctuation
- no leading or trailing hyphens

Edit only `slugify.mjs`. When you think it is done, stop.
